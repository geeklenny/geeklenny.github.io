.custom-logo {
  .site-meta-headline { text-align: center; }

  .brand { background: none; }

  .site-title {
    margin: 10px auto 0;
    font-size: 40px;
    a { border: none; }
  }
}

.custom-logo-image {
  margin: 0 auto;
  max-width: 240px;
  height: 40px; // 根据实际图片调整
  background: white;
}
